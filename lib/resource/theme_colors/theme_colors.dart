import 'package:flutter/material.dart';

abstract class ThemeColors {
  static const primaryColor = Color(0xFF1DB954);
  static const secondaryColor = Colors.grey;
  static const foregroundColor = Colors.white;

  static const backgroundBlack = Color(0xFF191414);

  static const List<Color> backgroundGradient = [Color(0xFF808080), Color(0xFF606060), Color(0xFF404040), Color(0xFF191414)];
}
